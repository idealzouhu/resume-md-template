### 为什么使用 Markdown 写简历？

Markdown 只是「内容 + 轻量结构」的中间格式，最终展示给用户看的，几乎一定是 HTML（前端）。

Markdown 的优点主要有：

- 纯文本，好维护
- 语法简单，专注内容
- 可版本控制（Git）
- 可自动化生成 PDF / 网页



### 典型流程

```
Markdown (.md)
   ↓（解析 / 编译）
HTML (DOM)
   ↓（CSS 样式）
前端页面
   ↓（可选,浏览器打印）
PDF / 图片
```

